{
  "name": "parse-apache-directory-index",
  "version": "1.2.1",
  "license": "MIT",
  "repository": "juliangruber/parse-apache-directory-index",
  "description": "Parse an HTML Apache directory index",
  "scripts": {
    "test": "tap --no-check-coverage test/*.js && semistandard"
  },
  "dependencies": {
    "cheerio": "^1.0.0-rc.10"
  },
  "devDependencies": {
    "semistandard": "^16.0.1",
    "tap": "^15.0.9"
  }
}
